if (CRITERION_FOUND)
  add_executable(test_kv_scanner test_kv_scanner.c)
  target_link_libraries(test_kv_scanner libtest criterion syslogformat syslog-ng)
  target_include_directories(test_kv_scanner PUBLIC "${CMAKE_SOURCE_DIR}/libtest")
  target_include_directories(test_kv_scanner PUBLIC "${KV_SCANNER_INCLUDE_DIR}")
  set_property(TARGET test_kv_scanner APPEND PROPERTY LINK_FLAGS "-Wl,--no-as-needed")
  set_property(TARGET test_kv_scanner PROPERTY POSITION_INDEPENDENT_CODE FALSE)
  add_test (test_kv_scanner test_kv_scanner)
endif(CRITERION_FOUND)
